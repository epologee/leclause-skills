# eye-of-the-beholder (deprecated)

`eye-of-the-beholder` has moved to **`laicluse-agent-fieldkit`** as `eye-of-the-beholder@laicluse-agent-fieldkit`. The skills are unchanged: `eye-of-the-beholder`, `art-director`, and `visual-inspection`.

This package is now a tombstone: it ships no skills. Its only remaining behaviour is a SessionStart notice that points here.

## Migrate

```bash
claude plugins marketplace add epologee/laicluse-agent-fieldkit
claude plugins install eye-of-the-beholder@laicluse-agent-fieldkit
claude plugins uninstall eye-of-the-beholder@leclause
```
