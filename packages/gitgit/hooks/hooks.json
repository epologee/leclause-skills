{
  "description": "Tombstone: a single SessionStart notice that gitgit has moved to git-discipline@laicluse-agent-tools. No commit guards remain.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/deprecated-notice.sh" }
        ]
      }
    ]
  }
}
