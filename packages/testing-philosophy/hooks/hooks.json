{
  "description": "Tombstone: a single SessionStart notice that testing-philosophy has moved into the house-rules plugin at house-rules@laicluse-agent-fieldkit. No skills remain.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/deprecated-notice.sh" }
        ]
      }
    ]
  }
}
