name: tombstones

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check deprecated plugin tombstones
        run: |
          bin/check-tombstones-test.sh
          bin/check-tombstones
